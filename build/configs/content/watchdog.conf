#watchdog.conf file
# format: (no quotes used)
# path/filename, arguments, timeout(in Seconds) - timeout of 0 means only watch for process failure, no watchdog.
# '#' (comment) lines are skipped as well as blank lines.

# this is for testing.....
#/bin/sleep,15,0

# Start ZIOS Network Manager
/zioshome/zios/networkmanager,,90

# Start ZIOS
/zioshome/zios/zios,--config /zioshome/zios/config.json ,30

